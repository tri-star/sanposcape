/**
 * 履歴が0件のときの空状態文言。記録タブの「最近の散歩」（`RecentWalksSection`）と
 * 履歴一覧（`WalkHistoryListView`）の両方で同じ文言を出すため、定数として1箇所にまとめる
 * （逐語コピーによる文言のズレを防ぐ）。
 */
export const WALK_HISTORY_EMPTY_TITLE = "まだ散歩の記録がありません";

/** 上記タイトルに添える補足説明。 */
export const WALK_HISTORY_EMPTY_DESCRIPTION = "散歩を終えると、ここに記録が並びます。";
