/**
 * 1日の目標歩数（スタブ）。
 *
 * `data/` は「将来 API へ差し替わる暫定スタブの置き場」（docs/folder-structure.md）。
 * 目標歩数のユーザー設定機能は SS-42 のスコープ外のため、当面この固定値を使う。
 * 設定機能を実装する際は、この定数の参照元（`useHistorySummary`）だけを差し替えればよい。
 */
export const STUB_DAILY_STEP_GOAL = 8000;
