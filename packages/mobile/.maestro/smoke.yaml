# Maestro E2E スモークフロー（サンプル）
# 実行には Android エミュレータ/実機 + development build（Expo Go 不可）が必要。
# ローカル(Windows側 Android)やCIでのエミュレータ上で実行する。
#
#   maestro test .maestro/smoke.yaml
#
appId: com.example.sanposcape # TODO: app.json の android.package / ios.bundleIdentifier 確定後に更新
---
- launchApp
- assertVisible: "sanposcape"
