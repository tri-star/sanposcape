# 探索画面の表示確認。候補件数・地図描画は backend/Google/端末の状態に依存するため assert しない。
appId: com.sanposcape.app
tags:
  - smoke
---
- runFlow: subflows/sign-in.yaml
- assertVisible:
    id: "walk-start-duration-slider"
- assertVisible:
    id: "walk-start-begin"
